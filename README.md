# hik_camera_sdk_add_pyqt-sdk-stream-can-use-on-this-ptqt-
This project uses Python to connect PyQt and a Hikvision PTZ camera, resolving the issue of video streams directly from the SDK not playing in Qt. It also returns playback codes, allowing many SDK methods that require SDK player return codes to be implemented without interrupting the video stream.
